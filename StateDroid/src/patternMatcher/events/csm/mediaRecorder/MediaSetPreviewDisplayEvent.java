package patternMatcher.events.csm.mediaRecorder;

import patternMatcher.events.Event;

public class MediaSetPreviewDisplayEvent extends Event{

	public Event createEvent()
	{
		
		return new MediaSetPreviewDisplayEvent();
	}
}
